import { useState } from 'react'
import { BrowserRouter, Link, Navigate, Route, Routes } from 'react-router-dom'
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { Activity, AlertTriangle, ArrowUpRight, Bell, Building2, ChevronDown, ClipboardCheck, Clock3, FileText, Gauge, LayoutDashboard, Menu, MoreHorizontal, Search, Settings, ShieldCheck, Sparkles, Users, Wrench, X, Zap } from 'lucide-react'
import { useLiveSensors } from './hooks/useLiveSensors'
import SensorChart from './components/SensorChart'
import './App.css'

const navItems = [{ label: 'Dashboard', icon: LayoutDashboard, path: '/dashboard' }, { label: 'Assets', icon: Gauge, path: '/dashboard' }, { label: 'Maintenance', icon: Wrench, count: '8', path: '/dashboard' }, { label: 'Alerts', icon: Bell, count: '12', path: '/dashboard' }, { label: 'Analytics', icon: Activity, path: '/dashboard' }]
const maintenanceQueue = [{ asset: 'P-104', risk: '89%', priority: 'Critical', issue: 'Bearing degradation', downtime: '18h', cost: '₹48,000' }, { asset: 'P-087', risk: '63%', priority: 'High', issue: 'Motor overheating', downtime: '9h', cost: '₹26,000' }, { asset: 'P-112', risk: '31%', priority: 'Medium', issue: 'Vibration anomaly', downtime: '4h', cost: '₹11,000' }]

function Button({ children, variant = 'primary', icon: Icon, onClick }) { return <button type="button" className={`btn btn-${variant}`} onClick={onClick}>{Icon && <Icon size={16} strokeWidth={2} />}{children}</button> }
function Badge({ children, tone = 'neutral' }) { return <span className={`badge badge-${tone}`}>{children}</span> }
function StatusBadge({ status }) { return <Badge tone={status.toLowerCase()}><span className="status-dot" />{status}</Badge> }
function MetricCard({ label, value, detail, icon: Icon, tone = 'blue' }) { return <article className="metric-card"><div className={`metric-icon metric-${tone}`}><Icon size={18} /></div><div><p className="eyebrow">{label}</p><strong>{value}</strong><p className="metric-detail">{detail}</p></div></article> }
function Modal({ onClose }) { return <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><section className="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title" onMouseDown={(event) => event.stopPropagation()}><button className="icon-button modal-close" onClick={onClose} aria-label="Close dialog"><X size={18} /></button><div className="modal-mark"><Sparkles size={22} /></div><h2 id="modal-title">AI maintenance brief</h2><p>PredictiMAINT has reviewed the latest Pump P-104 signals. The failure probability is 89% within 6 hours, with bearing degradation as the likely cause.</p><div className="brief-row"><span>Recommended next action</span><strong>Inspect bearing assembly</strong></div><Button onClick={onClose}>Review alert</Button></section></div> }
function Sidebar({ mobileOpen, onClose }) { return <aside className={`sidebar ${mobileOpen ? 'sidebar-open' : ''}`}><div className="brand"><div className="brand-mark"><Zap size={18} fill="currentColor" /></div><div><strong>Predicti<span>MAINT</span></strong><small>Asset intelligence platform</small></div><button className="icon-button mobile-close" onClick={onClose} aria-label="Close navigation"><X size={18} /></button></div><div className="workspace-switcher"><Building2 size={16} /><span>Northbridge Campus</span><ChevronDown size={14} /></div><nav aria-label="Primary navigation"><p className="nav-label">Workspace</p>{navItems.map(({ label, icon: Icon, count, path }) => <Link to={path} key={label} className={`nav-item ${label === 'Dashboard' ? 'active' : ''}`} onClick={onClose}><Icon size={18} /><span>{label}</span>{count && <em>{count}</em>}</Link>)}<p className="nav-label nav-label-spaced">Manage</p><Link to="/dashboard" className="nav-item"><Users size={18} /><span>Technicians</span></Link><Link to="/dashboard" className="nav-item"><Settings size={18} /><span>Settings</span></Link></nav><div className="sidebar-footer"><div className="system-status"><span className="status-dot" />All systems operational</div><p>Last synced 2 min ago</p></div></aside> }
function TopBar({ onMenu, onBrief }) { return <header className="topbar"><button className="icon-button menu-trigger" onClick={onMenu} aria-label="Open navigation"><Menu size={20} /></button><div className="top-search"><Search size={17} /><input aria-label="Search assets" placeholder="Search assets, jobs, alerts..." /><kbd>⌘ K</kbd></div><div className="top-actions"><button className="icon-button" onClick={onBrief} aria-label="View AI brief"><Sparkles size={18} /></button><button className="icon-button notification-button" aria-label="Notifications"><Bell size={18} /><span /></button><div className="profile"><div className="avatar">MC</div><div className="profile-copy"><strong>Marcus Chen</strong><small>Operations lead</small></div><ChevronDown size={15} /></div></div></header> }
function PageHeader({ onBrief }) { return <div className="page-header"><div><div className="breadcrumb">Workspace <span>/</span> Dashboard</div><h1>What needs your attention right now?</h1><p>Live operational intelligence across Northbridge Campus.</p></div><div className="header-actions"><Button variant="secondary" icon={FileText}>Export report</Button><Button icon={Sparkles} onClick={onBrief}>AI brief</Button></div></div> }
function SensorCard({ sensor, onSelect, selected }) { const change = `${sensor.percentageChange >= 0 ? '+' : ''}${sensor.percentageChange.toFixed(1)}%`; return <button type="button" className={`sensor-card ${selected ? 'sensor-card-selected' : ''}`} onClick={() => onSelect(sensor.id)}><div className="sensor-card-heading"><div><div className="sensor-type"><span className="live-pulse" />LIVE <span>·</span> {sensor.type}</div><h3>{sensor.name}</h3></div><StatusBadge status={sensor.status} /></div><div className="sensor-reading"><div><strong>{sensor.formattedValue}</strong><span>{sensor.unit}</span><p>Current reading</p></div><div className={`sensor-change sensor-change-${sensor.status.toLowerCase()}`}>{change}<small>vs baseline</small></div></div><div className="sensor-sparkline"><ResponsiveContainer width="100%" height="100%"><AreaChart data={sensor.history}><Area type="monotone" dataKey="value" stroke={sensor.status === 'Critical' ? '#d34b53' : '#c88216'} fill={sensor.status === 'Critical' ? '#fff0f1' : '#fff5df'} strokeWidth={1.7} dot={false} isAnimationActive={false} /></AreaChart></ResponsiveContainer></div><div className="sensor-updated"><span><span className="status-dot" />Receiving data</span><span>{sensor.updatedAt.toLocaleTimeString([], { minute: '2-digit', second: '2-digit' })}</span></div></button> }
function CriticalAlert({ onView }) { return <section className="critical-alert"><div className="critical-icon"><AlertTriangle size={21} /></div><div className="critical-copy"><div className="critical-kicker">CRITICAL <span>— FAILURE IMMINENT</span></div><h2>Pump P-104</h2><strong>89% failure probability within 6 hours</strong><p>Triggered by: <span>Vibration rising</span> · <span>Temperature rising</span> · <span>Pressure falling</span> · <span>Humidity increasing</span> · <span>Water flow decreasing</span></p><p>Likely cause: <b>Bearing degradation</b> · Confidence: <b>91%</b></p></div><div className="critical-actions"><Button variant="secondary" onClick={onView}>View Asset</Button><Button>Create Maintenance Job</Button></div></section> }
function SensorDetail({ sensor, windowSize, setWindowSize }) { const windows = [{ label: 'Last 1 minute', size: 40 }, { label: 'Last 5 minutes', size: 60 }, { label: 'Last 15 minutes', size: 60 }, { label: 'Last 1 hour', size: 60 }]; return <section className="panel sensor-detail"><div className="panel-heading"><div><p className="eyebrow">Selected sensor · Pump P-104</p><h2>{sensor.name}</h2></div><StatusBadge status={sensor.status} /></div><div className="detail-reading"><strong>{sensor.formattedValue}</strong><span>{sensor.unit}</span><Badge tone={sensor.status.toLowerCase()}>{sensor.percentageChange >= 0 ? '+' : ''}{sensor.percentageChange.toFixed(1)}% vs baseline</Badge></div><div className="chart-key"><span><i className="key-live" />Actual value</span><span><i className="key-baseline" />Baseline</span><span><i className="key-warning" />Warning / critical thresholds</span></div><SensorChart sensor={sensor} windowSize={windowSize} /><div className="chart-controls">{windows.map((item) => <button type="button" key={item.label} className={windowSize === item.size ? 'active' : ''} onClick={() => setWindowSize(item.size)}>{item.label}</button>)}</div><dl className="detail-meta"><div><dt>Baseline</dt><dd>{sensor.baseline} {sensor.unit}</dd></div><div><dt>Normal range</dt><dd>{sensor.normalRange}</dd></div><div><dt>Warning threshold</dt><dd>{sensor.warningThreshold} {sensor.unit}</dd></div><div><dt>Critical threshold</dt><dd>{sensor.criticalThreshold} {sensor.unit}</dd></div><div><dt>Last updated</dt><dd>{sensor.updatedAt.toLocaleTimeString()}</dd></div></dl></section> }
function Queue() { return <section className="panel queue-panel"><div className="panel-heading"><div><p className="eyebrow">Act · maintenance queue</p><h2>Priority actions</h2></div><Button variant="ghost" icon={ArrowUpRight}>View queue</Button></div><div className="table-scroll"><table><thead><tr><th>Asset</th><th>Risk</th><th>Priority</th><th>Issue</th><th>Downtime</th><th>Cost</th></tr></thead><tbody>{maintenanceQueue.map((item) => <tr key={item.asset}><td><strong>{item.asset}</strong></td><td><strong className="health-critical">{item.risk}</strong></td><td><StatusBadge status={item.priority === 'High' ? 'Warning' : item.priority === 'Medium' ? 'Normal' : 'Critical'} /></td><td>{item.issue}</td><td>{item.downtime}</td><td>{item.cost}</td></tr>)}</tbody></table></div></section> }
function RiskAndDistribution({ sensors }) { const vibration = sensors.find((sensor) => sensor.id === 'vibration'); const liveRisk = vibration.history.map((point) => ({ label: point.timestamp.getTime(), risk: Math.min(100, Math.max(0, 40 + ((point.value - vibration.baseline) / (vibration.criticalThreshold - vibration.baseline)) * 49)) })); const distribution = [{ label: 'Normal', value: 91, color: '#159a70' }, { label: 'Warning', value: 24, color: '#c88216' }, { label: 'Critical', value: 12, color: '#d34b53' }]; return <section className="lower-grid"><article className="panel risk-panel"><div className="panel-heading"><div><p className="eyebrow">Predict · live derived risk</p><h2>P-104 failure risk trend</h2></div><Badge tone="critical">89% · 6h window</Badge></div><div className="risk-chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={liveRisk}><defs><linearGradient id="liveRiskFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#d34b53" stopOpacity={0.24} /><stop offset="100%" stopColor="#d34b53" stopOpacity={0} /></linearGradient></defs><XAxis dataKey="label" type="number" domain={['dataMin', 'dataMax']} tickFormatter={(value) => new Date(value).toLocaleTimeString([], { minute: '2-digit', second: '2-digit' })} axisLine={false} tickLine={false} tick={{ fill: '#8b98a9', fontSize: 10 }} /><YAxis domain={[0, 100]} tickFormatter={(value) => `${value}%`} axisLine={false} tickLine={false} tick={{ fill: '#8b98a9', fontSize: 10 }} width={34} /><CartesianGrid stroke="#edf1f4" vertical={false} /><Tooltip labelFormatter={(value) => new Date(value).toLocaleTimeString()} formatter={(value) => [`${Number(value).toFixed(0)}%`, 'Failure risk']} /><Area type="monotone" dataKey="risk" stroke="#d34b53" strokeWidth={2.5} fill="url(#liveRiskFill)" dot={false} isAnimationActive={false} /></AreaChart></ResponsiveContainer></div></article><article className="panel distribution-panel"><div className="panel-heading"><div><p className="eyebrow">Fleet health</p><h2>Asset distribution</h2></div><Gauge size={18} className="muted" /></div><div className="distribution-chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={distribution} layout="vertical" margin={{ left: 5, right: 16 }}><XAxis type="number" hide /><YAxis dataKey="label" type="category" axisLine={false} tickLine={false} tick={{ fill: '#718096', fontSize: 11 }} width={58} /><Bar dataKey="value" radius={[0, 4, 4, 0]} barSize={14}>{distribution.map((item) => <Cell key={item.label} fill={item.color} />)}</Bar></BarChart></ResponsiveContainer></div><div className="distribution-total"><strong>127</strong><span>Assets monitored</span></div></article></section> }
function ActivityPanel() { return <section className="lower-grid activity-grid"><article className="panel"><div className="panel-heading"><div><p className="eyebrow">Explain · anomalies</p><h2>Recent anomalies</h2></div><MoreHorizontal size={18} className="muted" /></div><div className="activity-list"><div><span className="activity-dot critical" /><div><strong>Bearing degradation signature detected</strong><p>P-104 · 2 minutes ago</p></div><Badge tone="critical">Critical</Badge></div><div><span className="activity-dot warning" /><div><strong>Temperature trend exceeded model band</strong><p>CH-04 · 18 minutes ago</p></div><Badge tone="warning">Review</Badge></div></div></article><article className="panel"><div className="panel-heading"><div><p className="eyebrow">Verify · completed work</p><h2>Recent maintenance activity</h2></div><MoreHorizontal size={18} className="muted" /></div><div className="activity-list"><div><span className="activity-icon"><ClipboardCheck size={14} /></span><div><strong>Filter replacement completed</strong><p>AHU-21 · Today, 09:42</p></div><Badge tone="healthy">Verified</Badge></div><div><span className="activity-icon"><Wrench size={14} /></span><div><strong>Lubrication service logged</strong><p>P-098 · Yesterday, 16:20</p></div><Badge tone="neutral">Closed</Badge></div></div></article></section> }

function Dashboard() { const [mobileOpen, setMobileOpen] = useState(false); const [showModal, setShowModal] = useState(false); const [selectedId, setSelectedId] = useState('vibration'); const [windowSize, setWindowSize] = useState(60); const { sensors, connectedCount } = useLiveSensors(); const selectedSensor = sensors.find((sensor) => sensor.id === selectedId) || sensors[2]; return <div className="app-shell"><Sidebar mobileOpen={mobileOpen} onClose={() => setMobileOpen(false)} /><div className="main-area"><TopBar onMenu={() => setMobileOpen(true)} onBrief={() => setShowModal(true)} /><main><PageHeader onBrief={() => setShowModal(true)} /><CriticalAlert onView={() => setSelectedId('vibration')} /><section className="metric-grid"><MetricCard label="Fleet health score" value="72/100" detail="Pump P-104 · critical" icon={ShieldCheck} tone="amber" /><MetricCard label="Assets monitored" value="127" detail="91 normal · 24 warning" icon={Gauge} /><MetricCard label="Assets at risk" value="12" detail="3 require immediate action" icon={AlertTriangle} tone="red" /><MetricCard label="Downtime avoided" value="210 hrs" detail="This month · estimated" icon={Clock3} tone="violet" /></section><section className="sensor-section"><div className="sensor-section-heading"><div><p className="eyebrow">Collect · live telemetry</p><h2>Live Sensor Monitoring</h2><p>Five primary signals continuously feeding the predictive model.</p></div><div className="connected-status"><span className="live-pulse" />LIVE <strong>{connectedCount} Sensors Connected</strong><span className="connected-copy">— Receiving simulated sensor data</span></div></div><div className="sensor-grid">{sensors.map((sensor) => <SensorCard key={sensor.id} sensor={sensor} onSelect={setSelectedId} selected={sensor.id === selectedId} />)}</div></section><SensorDetail sensor={selectedSensor} windowSize={windowSize} setWindowSize={setWindowSize} /><Queue /><RiskAndDistribution sensors={sensors} /><ActivityPanel /></main><footer className="app-footer"><span><ClipboardCheck size={14} /> Predictive engine synced</span><span>v1.1 live monitoring</span></footer></div>{showModal && <Modal onClose={() => setShowModal(false)} />}</div> }

export default function App() { return <BrowserRouter><Routes><Route path="/dashboard" element={<Dashboard />} /><Route path="*" element={<Navigate to="/dashboard" replace />} /></Routes></BrowserRouter> }
